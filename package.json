{
  "name": "digital-clock-app",
  "version": "1.0.0",
  "description": "Full screen digital clock mobile app",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android": "cap run android",
    "build:android": "npm run build && cap sync && cap run android",
    "ios": "cap run ios",
    "build:ios": "npm run build && cap sync && cap run ios",
    "cap:sync": "cap sync",
    "cap:open": "cap open android",
    "cap:open:ios": "cap open ios"
  },
  "dependencies": {
    "vue": "^3.4.21",
    "@capacitor/core": "^5.7.0",
    "@capacitor/android": "^5.7.0",
    "@capacitor/ios": "^5.7.0",
    "@capacitor/app": "^5.0.6",
    "@capacitor/haptics": "^5.0.6",
    "@capacitor/keyboard": "^5.0.6",
    "@capacitor/status-bar": "^5.0.6"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.1.6"
  }
}